{
  "name": "securebank-api",
  "version": "1.0.0",
  "description": "SecureBank API - proyecto DevSecOps",
  "scripts": {
    "build": "mkdir -p dist && cp README.md dist/README.md",
    "test": "node -e \"console.log('Unit tests completed successfully')\""
  }
}
